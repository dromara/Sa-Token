<?xml version='1.0' encoding='utf-8'?>
<project xmlns="http://maven.apache.org/POM/4.0.0"
	xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
	xsi:schemaLocation="http://maven.apache.org/POM/4.0.0 http://maven.apache.org/xsd/maven-4.0.0.xsd">
	<modelVersion>4.0.0</modelVersion>

	<parent>
		<groupId>cn.dev33</groupId>
		<artifactId>sa-token-testing</artifactId>
		<version>${revision}</version>
		<relativePath>../pom.xml</relativePath>
	</parent>
	<packaging>jar</packaging>

	<name>sa-token-integration-jboot</name>
	<artifactId>sa-token-integration-jboot</artifactId>
	<description>JBoot HTTP integration tests for sa-token-jboot-plugin (Undertow / annotation interceptor; no Redis)</description>

	<dependencies>
		<dependency>
			<groupId>cn.dev33</groupId>
			<artifactId>sa-token-jboot-plugin</artifactId>
		</dependency>
		<dependency>
			<groupId>cn.dev33</groupId>
			<artifactId>sa-token-core</artifactId>
			<scope>test</scope>
		</dependency>
		<dependency>
			<groupId>cn.dev33</groupId>
			<artifactId>sa-token-servlet</artifactId>
			<scope>test</scope>
		</dependency>
		<dependency>
			<groupId>io.jboot</groupId>
			<artifactId>jboot</artifactId>
			<scope>test</scope>
		</dependency>
		<!-- JBoot 3.14.4 需要 JFinal 4.9.22（setMaxPostSize(long)）；BOM 默认是 4.9.17 -->
		<dependency>
			<groupId>com.jfinal</groupId>
			<artifactId>jfinal</artifactId>
			<version>4.9.22</version>
			<scope>test</scope>
		</dependency>
		<!-- jfinal-undertow 2.x 需要 Servlet 4（MappingMatch）；BOM 默认是 3.1，这里盖掉 -->
		<dependency>
			<groupId>javax.servlet</groupId>
			<artifactId>javax.servlet-api</artifactId>
			<version>4.0.1</version>
			<scope>test</scope>
		</dependency>
	</dependencies>

	<build>
		<plugins>
			<plugin>
				<groupId>org.apache.maven.plugins</groupId>
				<artifactId>maven-surefire-plugin</artifactId>
				<configuration>
					<argLine>${argLine} -Dfile.encoding=UTF-8</argLine>
					<forkCount>1</forkCount>
					<!-- Undertow / SaManager / JBoot 占进程，测试类之间不复用 JVM -->
					<reuseForks>false</reuseForks>
					<runOrder>random</runOrder>
				</configuration>
			</plugin>
		</plugins>
	</build>

	<!-- JBoot CGLIB 代理在 JDK9+ 必须打开 java.lang -->
	<profiles>
		<profile>
			<id>jdk9-plus</id>
			<activation>
				<jdk>[9,)</jdk>
			</activation>
			<build>
				<plugins>
					<plugin>
						<groupId>org.apache.maven.plugins</groupId>
						<artifactId>maven-surefire-plugin</artifactId>
						<configuration>
							<argLine>${argLine} -Dfile.encoding=UTF-8 --add-opens java.base/java.lang=ALL-UNNAMED --add-opens java.base/java.lang.reflect=ALL-UNNAMED --add-opens java.base/java.util=ALL-UNNAMED --add-opens java.base/java.io=ALL-UNNAMED --add-opens java.base/java.text=ALL-UNNAMED --add-opens java.base/jdk.internal.loader=ALL-UNNAMED --add-opens java.base/jdk.internal.misc=ALL-UNNAMED</argLine>
							<forkCount>1</forkCount>
							<reuseForks>false</reuseForks>
							<runOrder>random</runOrder>
						</configuration>
					</plugin>
				</plugins>
			</build>
		</profile>
	</profiles>

</project>
